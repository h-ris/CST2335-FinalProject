<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.CST2335_FinalProject"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".FlightTracker"
            android:exported="true">
        </activity>

        <activity
            android:name=".CcyConverterMainActivity"
            android:exported="false">
        </activity>

        <activity
            android:name=".TriviaRoomActivity"
            android:exported="false" />

        <activity
            android:name=".BearImageMainActivity"
            android:exported="false"
            android:theme="@style/Theme.CST2335_BearActivity">
        </activity>
        <activity android:name=".SavedImagesActivity"
            android:label="Saved Images">
        </activity>

        <activity
            android:name=".SavedFlights"
            android:exported="false"
            >
        </activity>

    </application>

</manifest>